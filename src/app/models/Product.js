class Product {
  constructor({ title, link, price, store, state }) {
    this.title = title;
    this.link = link;
    this.price = price;
    this.store = store;
    this.state = state;
  }
}

export default Product;
